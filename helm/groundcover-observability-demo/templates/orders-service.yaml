{{- if .Values.ordersService.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: orders-service
  namespace: {{ .Values.global.namespace }}
  labels:
    app: orders-service
spec:
  type: {{ default "ClusterIP" .Values.ordersService.service.type }}
  selector:
    app: orders-service
  ports:
    - name: http
      port: {{ .Values.ordersService.service.port }}
      targetPort: {{ .Values.ordersService.service.port }}
      {{- if and (eq (default "ClusterIP" .Values.ordersService.service.type) "NodePort") .Values.ordersService.service.nodePort }}
      nodePort: {{ .Values.ordersService.service.nodePort }}
      {{- end }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: orders-service
  namespace: {{ .Values.global.namespace }}
  labels:
    app: orders-service
spec:
  replicas: {{ .Values.ordersService.replicaCount }}
  selector:
    matchLabels:
      app: orders-service
  template:
    metadata:
      labels:
        app: orders-service
    spec:
      containers:
        - name: orders-service
          {{- if .Values.global.image.registry }}
          image: {{ printf "%s/%s:%s" .Values.global.image.registry .Values.ordersService.image.name .Values.global.image.tag }}
          {{- else }}
          image: {{ printf "%s:%s" .Values.ordersService.image.name .Values.global.image.tag }}
          {{- end }}
          imagePullPolicy: {{ .Values.global.image.pullPolicy }}
          env:
            - name: SPRING_DATASOURCE_URL
              value: jdbc:postgresql://orders-postgres:5432/orders
            - name: SPRING_DATASOURCE_USERNAME
              value: orders
            - name: SPRING_DATASOURCE_PASSWORD
              value: orders
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: {{ include "gc.otlpEndpoint" . }}
            - name: OTEL_RESOURCE_ATTRIBUTES
              value: "service.name=orders-service,service.namespace={{ .Values.global.namespace }},{{ .Values.global.otlp.resourceAttributes }}"
            {{- $java := include "gc.javaAgentOpts" . }}
            {{- if $java }}
            - name: JAVA_TOOL_OPTIONS
              value: {{ $java | quote }}
            {{- end }}
          ports:
            - containerPort: {{ .Values.ordersService.service.port }}
          readinessProbe:
            httpGet:
              path: /orders/health
              port: {{ .Values.ordersService.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /orders/health
              port: {{ .Values.ordersService.service.port }}
            initialDelaySeconds: 30
            periodSeconds: 30
          resources:
{{- toYaml .Values.ordersService.resources | nindent 12 }}
{{- end }}
