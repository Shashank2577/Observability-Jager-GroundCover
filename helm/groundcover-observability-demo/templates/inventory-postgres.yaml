{{- if and .Values.inventoryService.enabled .Values.inventoryService.postgres.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: inventory-postgres
  namespace: {{ .Values.global.namespace }}
  labels:
    app: inventory-postgres
spec:
  selector:
    app: inventory-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-postgres
  namespace: {{ .Values.global.namespace }}
  labels:
    app: inventory-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: inventory-postgres
  template:
    metadata:
      labels:
        app: inventory-postgres
    spec:
      containers:
        - name: postgres
          image: {{ .Values.inventoryService.postgres.image }}
          env:
            - name: POSTGRES_USER
              value: inventory
            - name: POSTGRES_PASSWORD
              value: inventory
            - name: POSTGRES_DB
              value: inventory
          ports:
            - containerPort: 5432
          readinessProbe:
            tcpSocket:
              port: 5432
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            tcpSocket:
              port: 5432
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
{{- toYaml .Values.inventoryService.postgres.resources | nindent 12 }}
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: data
          emptyDir: {}
{{- end }}

