{{- if and .Values.global.createNamespace .Values.global.namespace }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.global.namespace }}
  labels:
    app: {{ include "gc.name" . }}
{{- end }}
